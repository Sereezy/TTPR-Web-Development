//Problem: reverse the order in a linked list 

//First, create a function
function reverseOrder(arr) {
    var numbers = [0];
}
// Creating a for loop to iterate into each part of the list 
for (let )