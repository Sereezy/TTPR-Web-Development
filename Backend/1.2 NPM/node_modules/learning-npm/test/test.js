/* eslint-env mocha */
const expect = require('chai').expect
const scraper = require('../scraper')

describe('Kleinanzeigen Scraper Test(s)', () => {
  it('returns promise', function () {
    expect(scraper()).to.be.an.instanceof(Promise)
  })
})
